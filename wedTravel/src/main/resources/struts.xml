<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
        "http://struts.apache.org/dtds/struts-2.0.dtd">
<struts>
    <!-- 请求参数的编码方式-->

    <constant name="struts.i18n.encoding" value="UTF-8"/>

    <!-- 指定被struts2处理的请求后缀类型。多个用逗号隔开-->

    <constant name="struts.action.extension" value="action,do,json"/>

    <!-- 当struts.xml改动后，是否重新加载。默认值为false(生产环境下使用),开发阶段最好打开  -->

    <constant name="struts.configuration.xml.reload" value="true"/>

    <!-- 是否使用struts的开发模式。开发模式会有更多的调试信息。默认值为false(生产环境下使用),开发阶段最好打开  -->

    <constant name="struts.devMode" value="false"/>

    <!-- 设置浏览器是否缓存静态内容。默认值为true(生产环境下使用),开发阶段最好关闭  -->

    <constant name="struts.serve.static.browserCache" value="false"/>

    <!-- 指定由spring负责action对象的创建

    <constant name="struts.objectFactory" value="spring" />

    -->

    <!-- 是否开启动态方法调用-->

    <constant name="struts.enable.DynamicMethodInvocation" value="false"/>

    <package name="default" extends="struts-default">
        <action name="recommend" class="com.wt.action.RecommendAction">
            <result name="success">/WEB-INF/jsp/index.jsp</result>
        </action>
        <action name="area" class="com.wt.action.AreaAction">
            <result name="success">/WEB-INF/jsp/list1.jsp</result>
        </action>
    </package>

    <package name="ajax" extends="json-default">
        <action name="*_Ajax" class="com.wt.action.RecommendAction"  method="{1}">
            <!-- 将返回类型设置为json -->
            <result type="json"></result>
        </action>
        <action name="*_AjaxArea" class="com.wt.action.AreaAction"  method="{1}">
                    <!-- 将返回类型设置为json -->
                    <result type="json"></result>
                </action>
    </package>
</struts>
